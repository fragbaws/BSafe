<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#ffb400</item>
        <item name="colorPrimaryDark">#B98400</item>
        <item name="colorAccent">#ffb400</item>
    </style>

    <style name="ProgressStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">#40000000</item>
        <item name="android:background">#ffffff</item>
        <item name="colorAccent">#ffb400</item>
    </style>

    <style name="AlertStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">#ffffff</item>
        <item name="android:background">#ffb400</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
